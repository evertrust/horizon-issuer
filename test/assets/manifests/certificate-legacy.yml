apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate-legacy
spec:
  commonName: certificate-legacy.org
  secretName: certificate-legacy
  issuerRef:
    group: horizon.evertrust.io
    kind: ClusterIssuer
    name: valid-clusterissuer
