apiVersion: horizon.evertrust.io/v1beta1
kind: ClusterIssuer
metadata:
  name: clusterissuer-with-serviceaccount-badaudience
spec:
  url: http://horizon.horizon.svc.cluster.local:9000
  profile: issuer
  serviceAccount:
    name: horizon-issuer-tokenrequest
    serviceAccountRef:
      name: horizon-issuer-controller-manager
      audiences:
        - https://not-horizon.example.com
