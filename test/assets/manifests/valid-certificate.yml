apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: valid-certificate
spec:
  commonName: valid-certificate.org
  secretName: valid-certificate
  issuerRef:
    group: horizon.evertrust.io
    kind: ClusterIssuer
    name: valid-clusterissuer
