apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate-to-renew
spec:
  commonName: certificate-to-renew.org
  secretName: certificate-to-renew
  issuerRef:
    group: horizon.evertrust.io
    kind: ClusterIssuer
    name: valid-clusterissuer
