apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate-approved-concurrently
spec:
  commonName: certificate-approved-concurrently.org
  secretName: certificate-approved-concurrently
  issuerRef:
    group: horizon.evertrust.io
    kind: ClusterIssuer
    name: valid-clusterissuer
