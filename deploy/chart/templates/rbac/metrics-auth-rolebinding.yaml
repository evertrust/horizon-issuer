{{- if .Values.metrics.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
    name: horizon-issuer-metrics-auth-rolebinding
roleRef:
    apiGroup: rbac.authorization.k8s.io
    kind: ClusterRole
    name: horizon-issuer-metrics-auth-role
subjects:
    - kind: ServiceAccount
      name: horizon-issuer-controller-manager
      namespace: {{ .Release.Namespace }}
{{- end }}
